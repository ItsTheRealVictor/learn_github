name: test workflow

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  # workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: test
        run: echo "farts"
        shell: bash

  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: end test
        run: echo "poops"
        shell: bash
